package Noter_Vdr_Eksamen;


// sammenligner kandidater List<Candidate> comparePartyMembers = election.getCandidates().stream().sorted(Comparator.comparing(Candidate::getParty).thenComparing(Candidate::getName).thenComparing(Candidate::getTotalVotes)).toList();